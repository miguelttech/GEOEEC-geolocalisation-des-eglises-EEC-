# Framework principal
django==5.0.*
djangorestframework==3.15.*
djangorestframework-gis==1.0.*

# Base de données
psycopg2-binary==2.9.*
# GDAL sera résolu depuis l'installation système (apt-get)

# CORS (autoriser Next.js à appeler l'API)
django-cors-headers==4.3.*

# Cache Redis
django-redis==5.4.*

# Sécurité
django-ratelimit==4.1.*
argon2-cffi==23.*

# API documentation
drf-spectacular==0.27.*

# Import/Export données
openpyxl==3.1.*
Pillow==10.*

# PDF export
weasyprint==62.*

# Audit trail
django-simple-history==3.5.*

# Linting et qualité (dev)
ruff==0.4.*
black==24.*
pytest==8.*
pytest-django==4.8.*

# Production — serveur WSGI + fichiers statiques (avant mise en place du
# reverse proxy définitif) — voir docker-compose.prod.yml
gunicorn==22.*
whitenoise==6.*
