# Fichier vide monté à la place de /etc/nginx/conf.d/default.conf (livré par
# l'image nginx officielle) — sans ça, son "server { listen 80; ... }" par
# défaut entre en conflit avec infra/nginx/templates/geoeec.conf.template et
# sert la page d'accueil nginx au lieu de notre configuration (constaté lors
# de la vérification de l'INFRA-2).
